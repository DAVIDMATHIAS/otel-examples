management:
  endpoints:
    web:
      exposure:
        include: metrics,health,info
  otlp:
    metrics:
      export:
        enabled: true
        url: http://localhost:4318/v1/metrics
        step: 5s
otel:
  service:
    name: my-custom-service2-java